{
    "Version": "2012-10-17",
    "Statement": [
        {
            "Sid": "AllowDynamoDB",
            "Action": [
                "dynamodb:*"
            ],
            "Effect": "Allow",
            "Resource": "${dynamodb_arn}"
        }
    ]
}